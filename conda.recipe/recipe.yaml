# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
context:
  version: "0.3.0"

package:
  name: mojo-avro
  version: ${{ version }}

source:
  path: ..

build:
  number: 0
  script:
    - mkdir -p ${{ PREFIX }}/lib/mojo ${{ PREFIX }}/bin
    - mojo precompile -I src src/wire -o ${{ PREFIX }}/lib/mojo/wire.mojoc
    - mojo precompile -I src src/json -o ${{ PREFIX }}/lib/mojo/json.mojoc
    - mojo precompile -I src src/deflate -o ${{ PREFIX }}/lib/mojo/deflate.mojoc
    - mojo precompile -I src src/schema -o ${{ PREFIX }}/lib/mojo/schema.mojoc
    - mojo precompile -I src src/runtime -o ${{ PREFIX }}/lib/mojo/runtime.mojoc
    - mojo precompile -I src src/avro -o ${{ PREFIX }}/lib/mojo/avro.mojoc
    - mojo build -I src src/codegen/cli.mojo -o ${{ PREFIX }}/bin/gld-avrogen-mojo

requirements:
  build:
    - mojo-compiler ==1.0.0
  host:
    - mojo-compiler ==1.0.0
  run:
    - ${{ pin_compatible('mojo-compiler') }}

tests:
  - script:
      - if: unix
        then:
          - mojo run test_import.mojo
          - gld-avrogen-mojo --help
    files:
      recipe:
        - test_import.mojo

about:
  homepage: https://leo-gan.github.io/gld-avro/
  repository: https://github.com/leo-gan/gld-avro
  documentation: https://leo-gan.github.io/gld-avro/
  license: MIT
  license_file: LICENSE
  summary: From-scratch Apache Avro library for Mojo
  description: |
    Runtime and code generator written in Mojo. Does not wrap libavro
    or any other C, C++, or Rust Avro library. The package installs
    avro.mojoc (plus wire/json/schema/deflate/runtime) and the
    gld-avrogen-mojo CLI.
